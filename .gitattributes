# Keep generated output byte-stable across Windows CI and local builds,
# so a diff only ever reflects a real data change.
*.html text eol=lf
*.ps1  text eol=crlf
*.md   text eol=lf
*.yml  text eol=lf
